<!-- <?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="email_template_student_report" model="mail.template">
    <field name="name">Student Report Email</field>
    <field name="model_id" ref="student_management.model_student_student"/>
    <field name="subject">Student Report</field>
    <field name="email_from">${(user.email_formatted)}</field>
    <field name="email_to">${object.email}</field>

    <field name="body_html">
        <![CDATA[
            <p>Hello,</p>
            <p>Please find the attached student report.</p>
        ]]>
    </field>

   
    <field name="report_template_ids"
        eval="[(4, ref('student_management.action_student_report'))]"/>
</record>


</odoo> -->
