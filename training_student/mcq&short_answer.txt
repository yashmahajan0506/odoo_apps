1.Which Odoo decorator is used to compute a field only when dependencies change?
a) @api.onchange
b) @api.depends
c) @api.constrains
d) @api.model

ans:- b) @api.depends

2.  Odoo, ir.model.access.csv defines:
a) Data records 
b) Record rules
c) Access control lists (ACLs)
d) Scheduled actions

ans:- c) Access control lists (ACLs)

3.Which field type is best suited to store a file in Odoo?
a) Char
b) Binary
c) Text
d) Selection

ans;- b) Binary

4.The chatter functionality is enabled by inheriting which mixin?
a) mail.thread
b) mail.channel
c) mail.activity
d) mail.partnern

ans:- a) mail.thread



1.Explain the difference between related fields and computed fields in Odoo.:-

computed fields :-  a filed where value computed with python method 
relate filed:- a filed that shows value from another field with related model 

2.What is the role of Record Rules compared to Access Rights?
access Rights :- it is use for model level security and it can read,write,delte,update
records rule :-  it is use for on the records level of security  and  just defin what user can see or not 


3.What is the return of some orm method add any 5 orm return.
create:-  return a new recordset 
write   :-update existing record 
unlick :-  delete the record 
search :-  search a record 
read :-  read filed value 


4. Define the purpose of @api.onchange and give a practical example.
:--

class StudentSubjectLine(models.Model):
    _name = 'student.subject.line'
    _description = 'Student Subject Marks'

    student_id = fields.Many2one('student.student', string="Student", ondelete="cascade")
    subject_id = fields.Many2one('school.subject', string="Subject", required=True)
    subject_code = fields.Char(related='subject_id.code', string="Subject Code", store=True)
    marks = fields.Float(string="Marks",default=False)
    grade = fields.Char(string="Grade")

    @api.onchange('marks')
    def _onchange_marks(self):
        for rec in self:
            if rec.marks is None:
                rec.grade = False
            elif rec.marks >= 90:
                rec.grade = "A"
            elif rec.marks >= 75:
                rec.grade = "B"
            elif rec.marks >= 60:
                rec.grade = "C"
            elif rec.marks >= 40:
                rec.grade = "D"
            else:
                rec.grade = "F"



5.Explain the difference between search and browse methods..


search :-  you can find with condition  & return whole recordset 

browse:-you can find something with only ids & Return only with specific id related


